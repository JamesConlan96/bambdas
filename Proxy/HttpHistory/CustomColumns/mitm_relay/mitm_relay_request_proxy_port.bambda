/* mitm_relay_request_proxy_port

Proxy custom column bambda to add a proxy port column for mitm_relay traffic

*/

// Set value of column
return requestResponse.request().headerValue("Host").split(":")[1];
