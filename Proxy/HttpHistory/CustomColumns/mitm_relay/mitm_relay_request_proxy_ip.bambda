/* mitm_relay_request_proxy_ip

Proxy custom column bambda to add a proxy IP column for mitm_relay traffic

*/

// Set value of column
return requestResponse.request().headerValue("Host").split(":")[0];
